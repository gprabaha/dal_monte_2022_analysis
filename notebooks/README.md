# notebooks

Exploratory notebooks live here. They are intentionally separated from `src/` so that
analysis code stays reproducible and scripts remain scriptable.

Keep notebooks lightweight: use them to inspect outputs or prototype ideas, then
move reusable logic into `src/`.
