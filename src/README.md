# src

Source code lives under `src/` to support editable installs (`pip install -e .`).
The package name is `dal_monte_2022_analysis`.

If you add new modules, keep them inside the package folder and update imports
to use the package namespace.
